import styled from "styled-components";

const Apropos = () => {

    return (
        <Wrapper>
            <h1>A propos</h1>
        </Wrapper>
    )
}

const Wrapper = styled.main`
`

export default Apropos;